/** @type {import('next').NextConfig} */
const nextConfig = {
  // No external image domains needed for the starter — add yours here as you go:
  // images: { domains: ['your-image-host.com'] },
}

module.exports = nextConfig
